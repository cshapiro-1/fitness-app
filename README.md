# Fitness App

Standalone fitness tracker (trainer dashboard) scaffolded to mirror the Wanderer's Sketchbook architecture.

Run locally:

```bash
cd fitness-app
npm install
npm run dev
```

Open http://localhost:5173/ in your browser.
